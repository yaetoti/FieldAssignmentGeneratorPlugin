<idea-plugin require-restart="true">
  <id>com.jetbrains.rider.plugins.fieldassignmentgenerator</id>
  <name>Field Assignment Generator</name>
  <version>_PLACEHOLDER_</version>
  <vendor url="https://github.com/yaetoti/FieldAssignmentGeneratorPlugin">yaetoti</vendor>
  <idea-version since-build="261" />

  <description>
<![CDATA[
<p>Generates field assignments for C++ structs and classes from code completion.</p>
<p><b>Alpha version</b>: Expect occasional bugs and unsupported language constructs while the plugin is being actively developed.</p>
<p><b>Experimental</b>: This plugin uses a custom build configuration and internal CLion Nova APIs because an official plugin template is not yet available.</p>

<p>Found a bug or have a feature request? Please report it on the <a href="https://github.com/yaetoti/FieldAssignmentGeneratorPlugin/issues">GitHub Issues page</a>.</p>


<h3>Example</h3>

<pre><code>
DXGI_ADAPTER_DESC desc;
desc.
</code></pre>

<p>↓</p>

<pre><code>
desc.Description = ;
desc.VendorId = ;
desc.DeviceId = ;
desc.SubSysId = ;
...
</code></pre>

<h3>Features</h3>
<ul>
  <li>Generates field assignments for public fields owned by the object itself</li>
  <li>Respects formatting settings</li>
  <li>Editable hotspots for quick value entry</li>
</ul>

<h3>In development</h3>
<ul>
  <li>Configuration</li>
  <li>Pointer support</li>
  <li>Customizable completion display order</li>
  <li>Generating assignments for inherited fields (optional)</li>
  <li>Generating assignments for fields that are private or protected but could be accessed from the completion context (optional)</li>
  <li>Comma separated generation (optional)</li>
</ul>
]]>
  </description>

  <depends>com.intellij.modules.clion</depends>

  <depends>com.intellij.modules.jetbrains</depends>
  <depends>com.intellij.modules.platform</depends>
  <depends>com.intellij.modules.lang</depends>

  <actions>
  </actions>

</idea-plugin>
